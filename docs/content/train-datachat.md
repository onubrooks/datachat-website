# Train DataChat

Use **Train DataChat** when an answer is weak/wrong.

Flow:
- Start from a response.
- Create or update a datapoint.
- Save metadata notes (`grain`, `exclusions`, `confidence_notes`).
- Retry the same question and compare output.
